# codesys source xml
xml: robin.xml

# data types map
types: types.yml

# folder paths
folders:
    templates: templates/
    package:
      root: ../../
      node: src/
      robin_inst: src/robin/
      msg: msg/
      structs: include/robin/

# file names
files:
    node: robin_node.cpp
    robin_inst: robin_inst.cpp
    structs: structs.h
