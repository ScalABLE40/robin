# config folder
config:
    root: ../../cfg/
    proj: codesys_project.xml   # codesys project xml
    lib: robin.xml              # codesys library xml
    types: types.yml            # data types map
    templates: templates.yml    # templates

# package folders
package:
    root: ../../../../robin_bridge_generated/
    node: src/
    insts: src/robin_bridge_generated/
    msg: msg/
    structs: include/robin_bridge_generated/
    cmakelists: CMakeLists.txt
    package_xml: package.xml

# source file names
src_files:
    node: robin_node.cpp
    insts: robin_inst.cpp  #TODO change to robin_insts.cpp
    structs: structs.h
